# Tag-triggered release: multi-arch (amd64 + arm64) OBS image pushed to GHCR.
# The package is PUBLIC (this repo is public, the image carries no secrets), so
# cluster + CI pulls are anonymous — no imagePullSecret. GITHUB_TOKEN can push
# packages in the same repo, so no extra secret is needed.
#
# Built matrix-on-native-runners (tripbot's pattern): each arch builds on its own
# native GitHub-hosted runner — no QEMU. amd64 uses Dockerfile (OBS from the
# obsproject PPA) on ubuntu-latest; arm64 uses Dockerfile.arm64 (OBS from source,
# COPYing out of the pre-baked ghcr.io/adanalife/obs-cef-base — rebuilt separately
# by obs-base.yml) on ubuntu-24.04-arm; a manifest job stitches the per-arch tags
# into the multi-arch list.
#
# NOTE: prod runs OBS on the amd64 minipc today; the arm64 image is built for
# parity and is ready if OBS ever lands on an arm64 node.
name: Release
on:
  push:
    tags:
      - v*
  # A manual re-deploy of an existing tag, for when the push-triggered run
  # failed on something outside the code (a registry hiccup, an expired
  # token). Pick the tag as the ref.
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: build (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            runner: ubuntu-latest
            dockerfile: Dockerfile
          - arch: arm64
            runner: ubuntu-24.04-arm
            dockerfile: Dockerfile.arm64
    steps:
      - uses: actions/checkout@v7

      - name: Resolve bare version
        id: ver
        run: echo "value=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Per-arch tags get a -<arch> suffix so the manifest job can combine them
      # into a multi-arch list under the un-suffixed tag. onlatest=true applies
      # the suffix to the `latest` tag too, so each arch publishes latest-<arch>
      # — the manifest job (also latest=true) stitches the un-suffixed :latest
      # from them. Without onlatest=true the suffix skips `latest` (each leg pushes
      # a bare single-arch :latest and no latest-<arch>), so the manifest step dies
      # on `latest-amd64: not found`.
      - name: Generate tags
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/adanalife/obs
          flavor: |
            latest=true
            suffix=-${{ matrix.arch }},onlatest=true
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern=v{{major}}.{{minor}}
            type=semver,pattern=v{{major}}

      - name: Build & push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          platforms: linux/${{ matrix.arch }}
          tags: ${{ steps.meta.outputs.tags }}
          push: true
          load: true
          build-args: |
            VERSION=${{ steps.ver.outputs.value }}
            SHA=${{ github.sha }}
          cache-from: type=gha,scope=obs-${{ matrix.arch }}
          cache-to: type=gha,mode=max,scope=obs-${{ matrix.arch }}

      - name: Verify version stamping
        run: |
          IMAGE="ghcr.io/adanalife/obs:${{ steps.ver.outputs.value }}-${{ matrix.arch }}"
          echo "version: $(docker run --rm --entrypoint cat "$IMAGE" /etc/tripbot/version)"
          echo "sha:     $(docker run --rm --entrypoint cat "$IMAGE" /etc/tripbot/sha)"
          test "$(docker run --rm --entrypoint cat "$IMAGE" /etc/tripbot/version)" = "${{ steps.ver.outputs.value }}"
          test "$(docker run --rm --entrypoint cat "$IMAGE" /etc/tripbot/sha)" = "${{ github.sha }}"

  manifest:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate tags
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/adanalife/obs
          flavor: latest=true
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern=v{{major}}.{{minor}}
            type=semver,pattern=v{{major}}

      - name: Create multi-arch manifest lists
        run: |
          while IFS= read -r tag; do
            [ -z "$tag" ] && continue
            echo "==> $tag = $tag-amd64 + $tag-arm64"
            docker buildx imagetools create -t "$tag" "$tag-amd64" "$tag-arm64"
          done <<< "${{ steps.meta.outputs.tags }}"

  # The GitHub Release is created by release-please when the release PR merges;
  # this workflow only builds the image + notifies.
  notify:
    needs: manifest
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      # Only the tag push announces. A workflow_dispatch run re-deploys a tag
      # that already shipped, which is operational rather than news. The alert
      # below stays unconditional: a re-deploy going red is exactly the case
      # worth paging on.
      - name: Discord notification
        if: github.event_name == 'push'
        # A rotated or deleted webhook returns 404 and must not fail a release
        # that already shipped; the notification is best-effort.
        continue-on-error: true
        uses: Ilshidur/action-discord@0.4.0
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          VERSION_TAG: ${{ github.ref_name }}
        with:
          # Link the tagged CHANGELOG.md, not a payload `compare` URL: one wording
          # fleet-wide, so a release reads the same whichever repo it came from.
          args: 'New release for [{{ EVENT_PAYLOAD.repository.full_name }}]({{ EVENT_PAYLOAD.repository.html_url }}): [{{ VERSION_TAG }}]({{ EVENT_PAYLOAD.repository.html_url }}/blob/{{ VERSION_TAG }}/CHANGELOG.md)'

  # Nothing else says a release went red. The release PR merged green long
  # before the tag ran, so the only record is this run's own entry in a run
  # list nobody is watching.
  #
  # `if: failure()` is what makes this the catch-all: it runs when any job
  # above failed, including when a later one was skipped as a result. `notify`
  # cannot trip it, since continue-on-error keeps a dead webhook out of that
  # job's status.
  #
  # Best-effort like the announcement -- a failure to report a failure should
  # not become a second failure.
  alert:
    needs: [build, manifest, notify]
    if: failure()
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Alert Discord on failure
        continue-on-error: true
        uses: Ilshidur/action-discord@0.4.0
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_ALERTS }}
          VERSION_TAG: ${{ github.ref_name }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        with:
          # Points at the log rather than naming what failed: any job above can
          # be the one, and naming it would go stale as the jobs change.
          args: 'Release failed for [{{ EVENT_PAYLOAD.repository.full_name }}]({{ EVENT_PAYLOAD.repository.html_url }}) at {{ VERSION_TAG }}: [run log]({{ RUN_URL }})'
