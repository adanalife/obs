The album bed now plays from a node-local volume instead of the NAS share. OBS composites video and plays the bed in one process, so a share that stopped answering blocked the whole render pipeline — on 2026-08-05 that took both prod streams dark for 9h41m while OBS reported itself as streaming and encoded zero frames.
