The release PR is staged (changelog built, `cdk8s/dist` re-synthed, CI fired) on every main push while its branch exists, not only on the runs where release-please itself changed it — the gap that let a release merge with a stale dist and no checks.
