The four remaining `bin/obs-*` break-glass helpers now have tests. Making them testable meant giving each a `main()` — matching `obs-browser-refresh`, which already had that shape — so all five helpers are now one shape and their decisions are exercised in CI rather than first discovered mid-incident.
