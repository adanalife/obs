ruff now covers the `bin/` helpers by an explicit path pattern rather than by pre-commit reading their shebangs, so they stay linted if one is ever converted to a `uv run --script` shebang. The comment claiming cdk8s was the only Python in the repo was already untrue — `script/obs_server.py` and both `bin/` helpers are Python too.
